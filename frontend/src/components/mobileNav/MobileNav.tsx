import { Sheet, SheetTrigger } from "../ui/sheet"

const MobileNav = () => {
  return (
    <Sheet>
        <SheetTrigger>
            
        </SheetTrigger>
    </Sheet>
  )
}

export default MobileNav